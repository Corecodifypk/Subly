org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -Dfile.encoding=UTF-8
android.useAndroidX=true

# Limit workers — avoids OOM on 16 GB RAM machines during parallel Kotlin/Java compiles.
org.gradle.parallel=false
org.gradle.workers.max=2

# Fix Kotlin incremental cache errors when project (D:) and Pub cache (C:) are on different drives.
kotlin.incremental=false
kotlin.incremental.useClasspathSnapshot=false

# Keep Kotlin compiler memory low so Gradle + Kotlin daemons fit in RAM.
kotlin.daemon.jvmargs=-Xmx1024m -XX:MaxMetaspaceSize=256m
kotlin.compiler.execution.strategy=in-process
